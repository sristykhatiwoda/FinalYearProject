@model IEnumerable<OnlineCourse.Models.EXM_ExamRulesRegulation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExamRulesRegulationID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExamRulesRegulationText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExamRulesRegulationID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExamRulesRegulationText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.ExamRulesRegulationID }) |
            @Html.ActionLink("Details", "Details", new {id=item.ExamRulesRegulationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExamRulesRegulationID})
        </td>
    </tr>
}

</table>
